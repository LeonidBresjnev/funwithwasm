package percentile.project.demo.tree

/*
@Composable
fun ExpandableItemContent(
    items: List<MenuData>,
    onClick: () -> Unit = {} ) {
    items.forEach { item ->

            if (item.parentId != null) {
                ExpandableItemWidget(
                    modifier = Modifier.padding(horizontal = 10.dp),
                    title = item.displayName ?: "",
                    size = item.children.size,
                    onClick = { if (item.children.size > 0) onClick() }
                ) {
                    if (item.children.isNotEmpty()) {
                        ExpandableItemContent(item.children)
                    }

            }
        }



    }

}*/